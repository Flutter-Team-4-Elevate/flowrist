import 'package:flutter/material.dart';

class CartTabView extends StatelessWidget {
  const CartTabView({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Cart Tab'));
  }
}
